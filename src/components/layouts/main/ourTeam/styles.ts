import { css } from '@emotion/react';

const ourTeamContainerStyle = css({
  display: 'flex',
  alignItems: 'center',
  flexDirection: 'column',
});

const ourTeamHeaderStyle = css({
  marginBottom: '40px',
});

const styles = {
  ourTeamContainerStyle,
  ourTeamHeaderStyle,
};

export default styles;
